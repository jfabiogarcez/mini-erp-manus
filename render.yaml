services:
  - type: web
    name: mini-erp-manus
    env: docker
    dockerfilePath: ./Dockerfile
    startCommand: pnpm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
